# grounding-verifier requirements.
#
# torch is NOT listed — it ships with the pytorch/pytorch:2.6.0 base
# image via conda. Pinning it here would re-fetch the 906 MB CUDA wheel
# on every build over the operator's slow link (the same trap
# documents-rag hit). transformers/HHEM load whatever torch the base
# provides.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
# Transformers stack is vendored ONLY here — agents talk to /verify
# over HTTP and never import it.
transformers==4.46.3
# HHEM-2.1-Open uses tokenizers >= 0.20 implicitly.
tokenizers>=0.20
# HF Hub for offline-mode model loading.
huggingface_hub==0.26.5
