VITE_API_URL=http://localhost:3001

# "Send to EPLAN" (Eplanix tab) needs no address here: the browser only ever
# talks to this app's own backend, which forwards to eplan-bridge-service.
# See simorgh-backend/.env.example (EPLAN_BRIDGE_URL) and
# simorgh-agent/docs/EPLAN_SEND.md.
