fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
elasticsearch==8.15.1
httpx==0.27.2
mcp==1.10.1
sse-starlette==2.1.3
psycopg[binary]==3.2.3
# Cross-encoder reranker for /search/content. The CrossEncoder import
# is wrapped in try/except inside app.py — if this dep is missing or
# the model can't load, the endpoint falls back to ES ordering
# unchanged. ~600 MB cached on first request.
sentence-transformers>=3.0.0
# Phase 4 dense encoder (bge-m3) and bge-reranker-v2-m3 both ship
# pickled weights. transformers>=4.46 refuses to call torch.load on
# pickle with torch<2.6 (CVE). Upgrade both so the reranker can load.
# The base image (pytorch/pytorch:2.5.0-cuda12.4-cudnn9-runtime) ships
# torch 2.5; pip pulls 2.6+ from the CUDA 12.4 wheel index.
torch>=2.6,<2.8
transformers>=4.46,<5
