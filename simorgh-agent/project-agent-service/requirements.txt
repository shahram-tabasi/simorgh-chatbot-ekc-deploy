fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart>=0.0.9
pydantic[email]>=2.7.0,<3.0.0
aiofiles==23.2.1

asyncpg>=0.29.0
psycopg2-binary>=2.9.9
redis>=5.0.0
qdrant-client==1.11.1
neo4j>=5.15.0
pymysql>=1.1.0

openai>=1.3.0
httpx>=0.25.0
requests>=2.31.0
mcp>=1.8.0

bcrypt>=4.1.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
aiohttp>=3.9.0

pandas>=2.0.0
openpyxl>=3.1.0
numpy<2.0.0

paramiko>=3.4.0
tenacity>=8.2.3
sentence-transformers>=3.0.0,<5
# Phase 4 dense encoder (bge-m3) ships pickled pytorch_model.bin. From
# transformers>=4.46 the library refuses to call torch.load on pickled
# weights with torch<2.6 (CVE: arbitrary pickle deserialisation under
# weights_only=True). Pin both up so bge-m3 loads either via
# safetensors (preferred when present) or the safer torch.load path.
torch>=2.6,<2.8
transformers>=4.46,<5
# fastembed for the BM25 sparse encoder in services/qdrant_hybrid.
# Pulls in onnxruntime; CPU-only path is ~120 MB. The hybrid path
# falls back to dense-only when this fails to import.
fastembed>=0.4.0
# pdf2image for services/vlm_verifier — renders a single PDF page to PNG
# bytes so Qwen-VL on .62 can cross-check extracted values against the
# source page. Requires poppler-utils system package in the container;
# render is wrapped in try/except so a missing poppler degrades to
# "page image unavailable" rather than crashing the agent loop.
pdf2image>=1.17.0
